# Copy to .env and fill from your hosted Supabase project
# (Dashboard → Project Settings → API). The publishable/anon key is public by
# design — security is enforced by RLS, so it's safe to ship in the client.
VITE_SUPABASE_URL=https://<project-ref>.supabase.co
VITE_SUPABASE_ANON_KEY=
